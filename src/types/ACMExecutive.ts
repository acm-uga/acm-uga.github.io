type ACMExecutive = {
    /** The ACM Executive's role. */
    role: string,
    /** The ACM Executive first and last name. */
    name: string,
    /** The ACM Executive's photo source. */
    img: string
};

export default ACMExecutive;